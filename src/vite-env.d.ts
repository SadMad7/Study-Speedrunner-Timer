/// <reference types="vite/client" />

// This line pulls in Vite's ambient type declarations. Among other things it
// teaches TypeScript about special imports like `import x from 'file?url'`,
// which we use to locate the pdf.js worker.
